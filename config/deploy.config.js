// const { deploy, ssh } = require('./deploy.config');

// const config = { deploy, ssh };

// export default config;

// console.log({ deploy, ssh });

import { deploy, ssh } from './config'

export default { deploy, ssh }