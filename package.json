{
  "name": "helper-bot",
  "version": "1.4.3",
  "description": "Monorepo for Helper discord bot",
  "author": "Erika Cudd",
  "private": true,
  "license": "MIT",
  "repository": "https://github.com/instantrahmen/HelperBot",
  "workspaces": [
    "packages/server",
    "packages/web-panel",
    "packages/common",
    "packages/pocketbase",
    "config"
  ],
  "scripts": {
    "dev": "pnpm run dev -w @helper/server",
    "web": "pnpm run dev -w @helper/web-panel",
    "build:server": "pnpm run build -w @helper/server",
    "build:web": "pnpm run build -w @helper/web-panel",
    "start": "pnpm run start -w @helper/server",
    "build": "pnpm run build:server",
    "post-deploy": "pnpm install && pnpm run build"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "node-scp": "^0.0.23",
    "yaml": "^2.4.1"
  }
}
