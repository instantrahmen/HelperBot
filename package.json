{
  "name": "fynni",
  "version": "1.1.0",
  "description": "Monorepo for Fynni",
  "author": "Erika Cudd",
  "private": true,
  "workspaces": [
    "./packages/server"
  ],
  "scripts": {
    "dev": "yarn workspace @fynni/server dev",
    "install": "yarn workspace @fynni/server install && echo Finished installing",
    "build": "yarn workspace @fynni/server build"
  },
  "devDependencies": {
    "pm2": "^5.1.2"
  }
}
