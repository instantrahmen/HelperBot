{
  "name": "helper-bot",
  "version": "1.3.0",
  "description": "Monorepo for Helper discord bot",
  "author": "Erika Cudd",
  "private": true,
  "workspaces": [
    "packages/server",
    "packages/web-panel",
    "packages/common"
  ],
  "scripts": {
    "dev": "npm run dev -w @helper/server",
    "web": "npm run dev -w @helper/web-panel",
    "build": "npm run build -w @helper/server",
    "pm2-dev": "pm2 start --env development"
  },
  "devDependencies": {
    "pm2": "^5.2.0"
  },
  "dependencies": {
    "dotenv": "^16.0.0"
  }
}
